 {% extends 'base.html.twig' %}

{% block title %}Changer de mot de passe - site des LP{% endblock %}



{% block body %}
{% if error %}

<div class="alert alert-primary" role="alert">
  {{ error }}
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>

{% endif %}

    {{ form_start(form_change_password) }}
    {{ form_row(form_change_password.password , {'label': 'Mot de passe actuel','attr' : {'placeholder' : "Mot de passe actuel" }}) }}
    {{ form_row(form_change_password.new_password , {'label': 'Nouveau mot de passe','attr' : {'placeholder' : "Nouveau mot de passe" }}) }}
    {{ form_row(form_change_password.confirm_password , {'label': 'Confirmation mot de passe','attr' : {'placeholder' : "Confirmation mot de passe" }}) }}

      <span>

          <button type="submit" class="btn btn-success">Changer le mot de passe</button>

    {{ form_end(form_change_password) }}

      <a class="btn btn-primary" href= {{ path('secretaire_account')}} role="button">Annuler</a>
    </span>

{% endblock %}
